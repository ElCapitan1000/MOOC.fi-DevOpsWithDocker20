I changed the USER for the frontend and backend.
frontend and backend running as USER app.
Check localhost, all buttons and sending and getting messages works.

See step 9/11 in file usedCommands.
Step 9/11 : RUN whoami
 ---> Running in 9d8e54c33ce3
app

See the Dockerfiles in the folders frontend and backend.

